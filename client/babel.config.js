module.exports = {
    presets: [
      "@babel/preset-env",  // Transpile modern JavaScript to be compatible with older environments
      "@babel/preset-react" // Allow JSX to be properly transformed
    ],
  };
  